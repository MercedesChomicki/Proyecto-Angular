export interface Product {
    id: number;
    name: String,
    type: String,
    description: String,
    price: number,
    stock: number,
    image: String,
    sale: boolean,
    quantity: number;
}